#include <stdio.h>
#include <stdlib.h>

int main()
{
    int hex = 0x40;  // en sistema decimal 64

    printf("El valor hexadecimal 0x40 en sistema decimal es %d", hex);  //imprimimos el valor en sistema decimal 64
    return 0;
}
