#include <stdio.h>
#include <stdlib.h>

int main()
{
    int salario = 1000; //declaración de una variable de tipo integer llamada salario inicializada con un un valor de 6000
    printf("Su salario es de %d euros", salario);
    return 0;
}
