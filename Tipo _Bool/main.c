#include <stdio.h>
#include <stdlib.h>

int main()
{
    _Bool valido = 1;  //variable booleana inicializada a true = 1

    if (valido == 1) {
        printf("El resultado es valido");
    }
    else{
        printf("El resultado no es valido");
    }

    return 0;
}
