#include <stdio.h>
#include <stdlib.h>

/*El operador unario (-) hace que podamos visualizar una variable positiva como negativa */

int main()
{
    int gasto = 75;
    printf("Se ha descontado %d de su saldo", -gasto); //operador unario menos (-)
    return 0;
}
